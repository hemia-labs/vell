export enum FieldType {
  TEXT = 'text',
  TEXTAREA = 'textarea',
  NUMBER = 'number',
  BOOLEAN = 'boolean',
  DATE = 'date',
  IMAGE = 'image',
  FILE = 'file',
  SELECT = 'select',
  RELATION = 'relation',
  JSON = 'json',
  RICHTEXT = 'richtext',
}
